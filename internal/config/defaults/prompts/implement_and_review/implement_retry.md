The reviewer inspected your previous attempt and reported gaps. Your full prior conversation — the original task, every file you wrote, every command you ran — is still in your context via the fork. You do not need it re-sent.

# Reviewer feedback

<review_feedback>
$(review_feedback)
</review_feedback>

# What to do

1. **Read the reviewer's `missing` list carefully.** That is the authoritative list of what's wrong. The `checklist` section gives you the context for each gap; the `missing` section is the punch list.
2. **Diagnose each gap.** For each item, work out whether it's a real mismatch between what you produced and what the task asked for, or whether the reviewer misread the state. If the latter, still address it — produce clearer evidence (a comment, a named file, a test) so the next review lands cleanly.
3. **Make the smallest changes that close every gap.** Do not rewrite the whole solution unless the underlying approach is actually wrong. Targeted edits are faster and less risky than wholesale rewrites.
4. **Re-run your self-check** with the changes applied. Confirm each specific gap is now addressed.
5. **Stop.** The reviewer runs again. If gaps remain, you will be invoked once more with fresh feedback.

Do not argue with the review in prose — just fix the gaps.
